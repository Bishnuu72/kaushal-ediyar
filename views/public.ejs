<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= owner.name %> - Premium Collection</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; background: #f8f9fa; }
    .hero { 
      background: linear-gradient(rgba(0,0,0,0.75), rgba(0,0,0,0.75)), url('<%= owner.heroBg %>') center/cover no-repeat;
      color: white; padding: 180px 0; text-align: center;
    }
    .profile-img { width: 180px; height: 180px; object-fit: cover; border: 8px solid rgba(255,255,255,0.3); box-shadow: 0 20px 40px rgba(0,0,0,0.5); }
    .card { border-radius: 20px; overflow: hidden; transition: 0.4s; }
    .card:hover { transform: translateY(-15px); box-shadow: 0 25px 50px rgba(0,0,0,0.2); }
    footer { background: #222; color: #aaa; padding: 40px 0; }
  </style>
</head>
<body>

  <section class="hero">
    <div class="container">
      <img src="<%= owner.photo %>" class="rounded-circle profile-img mb-4" alt="<%= owner.name %>">
      <h1 class="display-3 fw-bold mb-3"><%= owner.name %></h1>
      <p class="lead fs-3 mb-2"><%= owner.email %> • <%= owner.phone %></p>
      <p class="fs-4"><i class="bi bi-geo-alt-fill"></i> <%= owner.address %></p>
    </div>
  </section>

  <section class="container my-5 py-5">
    <h2 class="text-center display-5 mb-5 text-primary fw-bold">Exclusive Collection</h2>
    <div class="row g-5">
      <% if(products.length === 0) { %>
        <p class="text-center fs-3 text-muted">New arrivals coming soon...</p>
      <% } else { products.forEach(p => { %>
        <div class="col-md-6 col-lg-4">
          <div class="card h-100 shadow-lg">
            <img src="<%= p.image %>" class="card-img-top" style="height:300px; object-fit:cover;">
            <div class="card-body text-center p-4">
              <h4 class="fw-bold text-primary"><%= p.name %></h4>
              <p class="text-muted"><%= p.description %></p>
              <h3 class="text-danger fw-bold">$<%= Number(p.price).toFixed(2) %></h3>
            </div>
          </div>
        </div>
      <% })} %>
    </div>

    <div class="text-center mt-5">
      <a href="/login" class="btn btn-dark btn-lg px-5 py-3 rounded-pill shadow">
        <i class="bi bi-shield-lock"></i> Admin Login
      </a>
    </div>
  </section>

  <footer class="text-center">
    <div class="container">
      <p class="mb-0">&copy; <%= new Date().getFullYear() %> <%= owner.name %>. All Rights Reserved.</p>
      <p class="small text-muted">Premium Products • Handcrafted with Love</p>
    </div>
  </footer>

</body>
</html>