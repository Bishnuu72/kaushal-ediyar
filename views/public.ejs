<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= owner.name %> - Spectra HealthCare</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root { --cyan: #00d4ff; }

    body {
      font-family: 'Space Grotesk', sans-serif;
      background: #0d0d15;
      color: white;
      margin: 0;
    }

    /* HERO (unchanged) */
    .hero {
      min-height: 100vh;
      background: linear-gradient(rgba(0,0,0,0.88), rgba(0,0,0,0.78)),
                  url('<%= owner.heroBg %>') center/cover no-repeat fixed;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      position: relative;
      padding: 20px;
    }

    .profile-img {
      width: 280px; height: 280px;
      object-fit: cover;
      border-radius: 50%;
      border: 12px solid rgba(0, 212, 255, 0.3);
      box-shadow: 0 0 120px rgba(0, 212, 255, 0.5);
      transition: 0.6s;
    }

    @media (max-width: 768px) {
      .profile-img {
        width: 180px;
        height: 180px;
      }
    }

    h1 {
      font-size: 5rem;
      font-weight: 700;
      background: linear-gradient(90deg, #fff, #00d4ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    @media (max-width: 768px) {
      h1 { font-size: 3rem; }
    }

    .tagline { font-size: 1.6rem; color: var(--cyan); }

    @media (max-width: 768px) {
      .tagline { font-size: 1.2rem; }
    }

    .contact-line {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 5px 10px;
      font-size: 1.4rem;
      margin-bottom: 10px;
    }

    /* Scroll button desktop */
    .scroll-btn {
      position: absolute;
      right: 40px;
      bottom: 40px;
      width: 68px; height: 68px;
      background: rgba(255,255,255,0.15);
      border: 2px solid rgba(0,212,255,0.4);
      border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      cursor: pointer;
      backdrop-filter: blur(20px);
      box-shadow: 0 0 80px rgba(0,212,255,0.4);
      animation: float 6s ease-in-out infinite;
      z-index: 50;
      transition: 0.3s ease-in-out;
    }

    @keyframes float {
      0%,100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    /* Mobile scroll button reposition */
    @media (max-width: 768px) {
      .scroll-btn {
        position: static;
        margin: 20px auto 0 auto;
        width: 60px;
        height: 60px;
      }
    }

    /* PRODUCTS SECTION */
    .products-section {
      padding: 140px 0;
      background: #11121a;
    }

    .section-title {
      font-size: 3.5rem;
      text-align: center;
      margin-bottom: 80px;
      background: linear-gradient(90deg, #00d4ff, #fff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      line-height: 1.1;
    }

    /* ⭐ MOBILE FIX: FORCE THIS TITLE INTO 2 CLEAN LINES ⭐ */
    @media (max-width: 768px) {
      .section-title {
        font-size: 2.2rem;
        max-width: 90%;
        margin-left: auto;
        margin-right: auto;
        white-space: normal;
        text-align: center;
        display: block;
      }
    }

    .product-card {
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(0,212,255,0.25);
      border-radius: 28px;
      overflow: hidden;
      transition: 0.6s;
      backdrop-filter: blur(10px);
    }

    .product-card:hover {
      transform: translateY(-15px);
    }

    .product-img {
      width: 100%;
      height: auto;
      max-height: 470px;
      object-fit: contain;
      background: #0f0f17;
      padding: 20px;
      display: block;
    }

    .card-body {
      padding: 30px;
      background: rgba(10,10,20,0.9);
      text-align: center;
    }

    .product-name { font-size: 1.8rem; color: white; }
    .product-desc { font-size: 1.2rem; color: #ccc; }
    .product-price { font-size: 2.4rem; color: var(--cyan); }

    footer {
      background: #0d0d15;
      padding: 60px 0;
      border-top: 1px solid rgba(0,212,255,0.2);
      text-align: center;
    }

  </style>
</head>

<body>

  <!-- HERO -->
  <section class="hero">
    <div class="container">

      <img src="<%= owner.photo %>" class="profile-img" alt="<%= owner.name %>">

      <h1><%= owner.name %></h1>

      <p class="tagline"><%= owner.tagline || "Distributor of Azul Products - Spectra HealthCare" %></p>

      <p class="fs-4">
        <i class="bi bi-envelope"></i> <%= owner.email %>
      </p>

      <div class="contact-line">
        <span><i class="bi bi-phone"></i> <%= owner.phone %></span>
        <span><i class="bi bi-geo-alt-fill"></i> <%= owner.address %></span>
      </div>

      <div class="scroll-btn"
        onclick="document.querySelector('.products-section').scrollIntoView({behavior: 'smooth'})">
        <i class="bi bi-arrow-down"></i>
      </div>

    </div>
  </section>

  <!-- PRODUCTS -->
  <section class="products-section">
    <div class="container">

      <h2 class="section-title">Azul Products - Spectra HealthCare</h2>

      <div class="row g-5 justify-content-center">
        <% if(products.length === 0) { %>

          <h3 class="text-center" style="color:#00d4ff">New products coming soon...</h3>

        <% } else { products.forEach(p => { %>

          <div class="col-md-6 col-lg-4">
            <div class="product-card">
              <img src="<%= p.image %>" class="product-img" alt="<%= p.name %>">

              <div class="card-body">
                <h3 class="product-name"><%= p.name %></h3>
                <p class="product-desc"><%= p.description %></p>
                <h2 class="product-price">Rs <%= Number(p.price).toFixed(2) %></h2>
              </div>
            </div>
          </div>

        <% }) } %>
      </div>

      <div class="text-center mt-5">
        <a href="/login" class="btn btn-outline-light btn-lg px-5 py-3 rounded-pill"
           style="border-color:#00d4ff;color:#00d4ff;">
          ADMIN LOGIN
        </a>
      </div>

    </div>
  </section>

  <footer>
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> <%= owner.name %>. All Rights Reserved.</p>
      <p class="small text-muted">Premium Azul Products • Spectra HealthCare Nepal</p>
    </div>
  </footer>

</body>
</html>
