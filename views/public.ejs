<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= owner.name %> - Spectra HealthCare</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root { --cyan: #00d4ff; }
    body { font-family: 'Space Grotesk', sans-serif; background: #000; color: white; margin: 0; }
    
    .hero {
      min-height: 100vh;
      background: linear-gradient(rgba(0,0,0,0.88), rgba(0,0,0,0.78)), url('<%= owner.heroBg %>') center/cover no-repeat fixed;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      position: relative;
      padding: 0 20px;
    }

    .profile-img {
      width: 280px; height: 280px; object-fit: cover; border-radius: 50%;
      border: 12px solid rgba(0, 212, 255, 0.3);
      box-shadow: 0 0 120px rgba(0, 212, 255, 0.5);
      transition: 0.6s;
    }

    h1 { font-size: 5rem; font-weight: 700; background: linear-gradient(90deg, #fff, #00d4ff); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
    .tagline { font-size: 2rem; color: #00d4ff; letter-spacing: 2px; }

    .scroll-btn {
      position: absolute; right: 40px; bottom: 60px; width: 68px; height: 68px;
      background: rgba(255,255,255,0.15); border: 2px solid rgba(0,212,255,0.4);
      border-radius: 50%; display: flex; align-items: center; justify-content: center;
      cursor: pointer; backdrop-filter: blur(20px); box-shadow: 0 0 80px rgba(0,212,255,0.4);
      animation: float 6s ease-in-out infinite;
    }
    .scroll-btn i { font-size: 2rem; color: #00d4ff; }
    @keyframes float { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-30px); } }

    .products-section { padding: 160px 0; background: #000; }
    .section-title { font-size: 4rem; text-align: center; margin-bottom: 100px; background: linear-gradient(90deg, #00d4ff, #fff); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }

    .product-card {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(0,212,255,0.25);
      border-radius: 35px;
      overflow: hidden;
      transition: transform 0.6s;
      backdrop-filter: blur(10px);
    }
    .product-card:hover { transform: translateY(-20px); }

    /* FULL IMAGE - NO CROP, NO ZOOM */
    .product-img {
      width: 100%;
      height: auto;
      max-height: 500px;
      object-fit: contain;
      background: #111;
      padding: 20px;
    }

    .card-body {
      padding: 40px;
      text-align: center;
      background: rgba(10,10,20,0.95);
    }
    .product-name { font-size: 2.2rem; color: white; margin-bottom: 20px; }
    .product-desc { font-size: 1.3rem; color: #ccc; margin-bottom: 30px; line-height: 1.7; }
    .product-price { font-size: 2.8rem; color: #00d4ff; font-weight: 700; }

    footer { background: #000; padding: 60px 0; border-top: 1px solid rgba(0,212,255,0.2); text-align: center; }
  </style>
</head>
<body>

  <section class="hero">
    <div class="container">
      <img src="<%= owner.photo %>" class="profile-img" alt="<%= owner.name %>">
      <h1><%= owner.name %></h1>
      <p class="tagline"><%= owner.tagline || "Distributor of Azul Products - Spectra HealthCare" %></p>
      <p class="fs-3 mb-2"><i class="bi bi-envelope"></i> <%= owner.email %></p>
      <p class="fs-3"><i class="bi bi-phone"></i> <%= owner.phone %> • <i class="bi bi-geo-alt-fill"></i> <%= owner.address %></p>

      <div class="scroll-btn" onclick="document.querySelector('.products-section').scrollIntoView({behavior: 'smooth'})">
        <i class="bi bi-arrow-down"></i>
      </div>
    </div>
  </section>

  <section class="products-section">
    <div class="container">
      <h2 class="section-title">Azul Products - Spectra HealthCare</h2>
      
      <div class="row g-5 justify-content-center">
        <% if(products.length === 0) { %>
          <h3 class="text-center" style="color:#00d4ff">New products coming soon...</h3>
        <% } else { products.forEach(p => { %>
          <div class="col-md-6 col-lg-4">
            <div class="product-card">
              <img src="<%= p.image %>" class="product-img" alt="<%= p.name %>">
              <div class="card-body">
                <h3 class="product-name"><%= p.name %></h3>
                <p class="product-desc"><%= p.description %></p>
                <h2 class="product-price">Rs <%= Number(p.price).toFixed(2) %></h2>
              </div>
            </div>
          </div>
        <% })} %>
      </div>

      <div class="text-center mt-5">
        <a href="/login" class="btn btn-outline-light btn-lg px-5 py-3 rounded-pill" style="border-color:#00d4ff;color:#00d4ff;">
          ADMIN LOGIN
        </a>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> <%= owner.name %>. All Rights Reserved.</p>
      <p class="small text-muted">Premium Azul Products • Spectra HealthCare Nepal</p>
    </div>
  </footer>

</body>
</html>