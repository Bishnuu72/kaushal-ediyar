<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= owner.name %> - Spectra HealthCare</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; background: #f8f9fa; }
    .hero { 
      background: linear-gradient(rgba(0,0,0,0.75), rgba(0,0,0,0.75)), url('<%= owner.heroBg %>') center/cover no-repeat;
      color: white; padding: 200px 0 150px; text-align: center;
    }
    .profile-img { width: 200px; height: 200px; object-fit: cover; border: 10px solid rgba(255,255,255,0.3); box-shadow: 0 20px 40px rgba(0,0,0,0.5); }
    .card { border-radius: 25px; overflow: hidden; transition: 0.4s; }
    .card:hover { transform: translateY(-20px); box-shadow: 0 30px 60px rgba(0,0,0,0.2); }
    footer { background: #111; color: #888; padding: 50px 0; font-size: 0.9rem; }
  </style>
</head>
<body>

  <section class="hero">
    <div class="container">
      <img src="<%= owner.photo %>" class="rounded-circle profile-img mb-4" alt="<%= owner.name %>">
      <h1 class="display-2 fw-bold mb-2"><%= owner.name %></h1>
      <p class="fs-3 mb-3 text-white-75"><%= owner.tagline || "Distributor of Azul Products - Spectra HealthCare" %></p>
      <p class="lead mb-2"><i class="bi bi-envelope"></i> <%= owner.email %></p>
      <p class="lead"><i class="bi bi-phone"></i> <%= owner.phone %> • <i class="bi bi-geo-alt-fill"></i> <%= owner.address %></p>
    </div>
  </section>

  <section class="container my-5 py-5">
    <h2 class="text-center display-4 mb-5 text-primary fw-bold">Azul Products - Spectra HealthCare</h2>
    <div class="row g-5">
      <% if(products.length === 0) { %>
        <p class="text-center fs-2 text-muted">New products coming soon...</p>
      <% } else { products.forEach(p => { %>
        <div class="col-md-6 col-lg-4">
          <div class="card h-100 shadow-lg">
            <img src="<%= p.image %>" class="card-img-top" style="height:320px; object-fit:cover;">
            <div class="card-body text-center p-4">
              <h3 class="fw-bold text-primary"><%= p.name %></h3>
              <p class="text-muted"><%= p.description %></p>
              <h2 class="text-danger fw-bold">Rs <%= Number(p.price).toFixed(2) %></h2>
            </div>
          </div>
        </div>
      <% })} %>
    </div>

    <div class="text-center mt-5">
      <a href="/login" class="btn btn-dark btn-lg px-5 py-3 rounded-pill shadow">
        <i class="bi bi-shield-lock"></i> Admin Login
      </a>
    </div>
  </section>

  <footer class="text-center">
    <div class="container">
      <p class="mb-0">&copy; <%= new Date().getFullYear() %> <%= owner.name %>. All Rights Reserved.</p>
      <p class="small text-muted">Premium Azul Products • Spectra HealthCare Nepal</p>
    </div>
  </footer>

</body>
</html>